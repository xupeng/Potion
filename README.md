# Potion - A Personalized Notion

Potion now supports:
1. Multiple tabs (Mac only).
1. CSS injection.
1. "Open in Mac app".

Run

```bash
npm install
npm start
```

Build

```bash
npm install
npm run build
```